node_modules/
dist/
.next/
.turbo/
pnpm-lock.yaml
packages/db/generated/
playwright-report/
test-results/

# Specification and planning documents are authored by hand and are the
# source of truth for this build. Prettier reflows markdown lists and headings,
# which renumbers rules and creates spurious diffs in the one place where a
# rule number has to stay stable.
master-prompt.md
report.md
prompt_phase1.md
prompt_phase2.md

# Next.js rewrites this file's `include` array during build.
apps/web/tsconfig.json
